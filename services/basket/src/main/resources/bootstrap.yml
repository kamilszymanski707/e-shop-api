spring:
  application:
    name: basket
  cloud:
    config:
      uri: ${CONFIG_URI:http://127.0.0.1:8888}

server:
  port: ${PORT:0}

logstash.host: ${LOGSTASH_HOST:127.0.0.1}
logstash.port: ${LOGSTASH_PORT:5000}
